import 'package:flutter/material.dart';

class TradeSpesificPages extends StatelessWidget {
  const TradeSpesificPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Trade')),
    );
  }
}
