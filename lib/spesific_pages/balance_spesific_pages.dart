import 'package:flutter/material.dart';

class BalanceSpesificPages extends StatelessWidget {
  const BalanceSpesificPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Balance')),
    );
  }
}
