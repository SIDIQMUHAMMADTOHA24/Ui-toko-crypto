import 'package:flutter/material.dart';

class SettingsSpesificPages extends StatelessWidget {
  const SettingsSpesificPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Settings')),
    );
  }
}
